import { PublicClient } from 'viem';
import { NetworkConfigurationMap } from '@dynamic-labs/types';
export * from '@dynamic-labs/rpc-providers';
type IEvmRpcProvider = {
    chainId: number;
    chainName: string;
    provider: PublicClient;
};
declare module '@dynamic-labs/rpc-providers' {
    type EvmRpcProvider = IEvmRpcProvider;
    interface RpcProviderLookup {
        evm: EvmRpcProvider[];
    }
    interface IChainRpcProviders {
        getEvmProviderByChainId(config: NetworkConfigurationMap, chainId: number): EvmRpcProvider | undefined;
        registerEvmProviders(): void;
    }
}
