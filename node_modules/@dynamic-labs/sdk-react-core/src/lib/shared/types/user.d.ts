import { JwtVerifiedCredential, NameServiceData, UserFields } from '@dynamic-labs/sdk-api';
export type UserProfile = UserFields & {
    chain?: string;
    ens?: NameServiceData;
    environmentId?: string;
    isAuthenticatedWithAWallet: boolean;
    lastVerifiedCredentialId: string | undefined;
    lists?: string[];
    newUser?: boolean;
    sessionId: string;
    userId?: string;
    verifiedCredentials: JwtVerifiedCredential[];
    wallet?: string;
    scope?: string;
};
