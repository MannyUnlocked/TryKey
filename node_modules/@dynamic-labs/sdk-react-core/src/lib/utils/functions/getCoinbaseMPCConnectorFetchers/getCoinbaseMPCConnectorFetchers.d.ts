type Props = {
    environmentId: string;
    authToken: string;
};
export declare const getCoinbaseMPCConnectorFetchers: ({ environmentId, authToken, }: Props) => {
    fetchAuthToken: () => Promise<string>;
    fetchBackupCode: () => Promise<string>;
    fetchPasscode: () => Promise<string>;
};
export {};
