'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var reactInternationalPhone = require('react-international-phone');

/** Whether phone number length matches the expectation for this country */
const validatePhoneLength = (phone, country) => reactInternationalPhone.removeDialCode({ dialCode: country.dialCode, phone }).length ===
    reactInternationalPhone.getActiveFormattingMask({
        country,
        disableFormatting: true,
        phone,
    }).length;

exports.validatePhoneLength = validatePhoneLength;
