import { UpdateSelfResponse } from '@dynamic-labs/sdk-api';
import { UpdateUserReturnPayload } from '../..';
export type VerifyEmailCallback = (verificationToken: string) => Promise<UpdateSelfResponse>;
export type VerifyOtp = (verificationToken: string, destination: 'email' | 'sms', verificationUUID?: string) => Promise<UpdateUserReturnPayload>;
export declare const useOtpVerificationRequest: () => {
    verifyOtp: VerifyOtp;
};
