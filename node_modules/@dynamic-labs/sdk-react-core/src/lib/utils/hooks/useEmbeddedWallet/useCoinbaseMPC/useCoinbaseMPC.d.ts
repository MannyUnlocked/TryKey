import { ChainEnum, DynamicJwt } from '@dynamic-labs/sdk-api';
import { Wallet } from '../../../../shared';
export declare const useCoinbaseMPC: () => {
    createCoinbaseMPCWallet: (jwt: string, decodedJwt: DynamicJwt, chain?: ChainEnum, password?: string) => Promise<Wallet | undefined>;
};
