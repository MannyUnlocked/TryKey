import { ProjectSettings } from '@dynamic-labs/sdk-api';
import { GenericNetwork } from '@dynamic-labs/types';
import { EvmNetworksOverrides } from '../../../context/DynamicContext/types';
export type FetchNetworkConfigurationsProps = {
    authToken: string | undefined;
    environmentId: string;
    evmNetworks?: GenericNetwork[];
    evmNetworksOverrides?: EvmNetworksOverrides;
    projectSettings?: ProjectSettings;
};
export declare const useProcessNetworkConfigurations: ({ authToken, environmentId, evmNetworksOverrides, projectSettings, }: FetchNetworkConfigurationsProps) => {
    handleRemoveLsNetworks: () => void;
    networkConfigurations: import("@dynamic-labs/types").NetworkConfigurationMap | undefined;
};
