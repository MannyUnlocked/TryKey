import { UserProfile } from '../../../shared';
type Props = {
    handleLogOut: () => Promise<void>;
    user: UserProfile | undefined;
    authToken: string | undefined;
};
export declare const useValidateSession: ({ handleLogOut, user, authToken, }: Props) => void;
export {};
